import { asyncRouterMap } from '../router';
import info_config from '../info.config';

const state: any = {

    /** 路由 **/
    routers: asyncRouterMap,

    /** 信息 **/
    info_config,

};

export default state;